import { Stack, Typography } from '@mui/material';
const Home = () => {
  return (
    <Stack spacing={2}>
      <Typography variant="h3" gutterBottom>
        Home Page
      </Typography>
    </Stack>
  );
};

export default Home;
